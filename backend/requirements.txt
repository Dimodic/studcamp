# Runtime-зависимости бэкенда. Версии закреплены для воспроизводимых сборок.
# Dev-инструменты (ruff, mypy, coverage, pre-commit) — см. requirements-dev.txt.
#
# Как обновлять:
#   python -m pip install --upgrade <pkg>
#   python -m pip freeze | grep <pkg>       # взять актуальную версию
#   # пропинуть здесь + pytest (включая backend/tests/) до коммита
fastapi==0.136.0
uvicorn[standard]==0.44.0
gunicorn==25.3.0
sqlalchemy==2.0.49
alembic==1.18.4
psycopg[binary]==3.3.3
pydantic==2.13.3
pydantic-settings==2.14.0
httpx==0.28.1
bcrypt==5.0.0
pytest==9.0.3
